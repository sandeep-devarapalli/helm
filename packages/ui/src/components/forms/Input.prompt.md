Text field with the console's quiet style — hairline border, accent focus ring.

```jsx
<Input label="Symbol" icon={<Search />} hint="460 factors indexed" />
```

Optional `label`, `icon`, `hint`, and `error` (switches to danger style). Sizes `sm | md | lg`. Forwards all native input props.
