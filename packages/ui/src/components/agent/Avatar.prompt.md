Avatar — the helm agent mark (ink rounded square with mono "h") or the neutral gray user circle with an initial.

```jsx
<Avatar role="agent" />
<Avatar role="user">D</Avatar>
```

`size` in px (default 28).
