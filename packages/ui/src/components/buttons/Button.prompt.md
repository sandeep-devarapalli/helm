Ink-black action button (light workbench hierarchy).

```jsx
<Button onClick={start}>Get started</Button>
<Button variant="secondary">Watch a run</Button>
```

Variants: `primary` (ink fill), `secondary` (white, bordered), `ghost`, `danger`. Sizes `sm | md | lg`; `loading`, `disabled`, `fullWidth`, `icon`/`iconRight`.
