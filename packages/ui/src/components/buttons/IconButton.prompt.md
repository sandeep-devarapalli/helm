Square, icon-only button for console chrome — sidebar toggles, copy actions, row controls.

```jsx
<IconButton title="New session" variant="solid"><Plus /></IconButton>
```

Sizes `sm | md | lg`. `variant="ghost"` (default) or `"solid"`. Set `active` for a selected/toggled state. Always pass `title` for accessibility.
