One Explorer watchlist row — mono ticker with a leading bank glyph; selected rows fill gray.

```jsx
<WatchlistRow symbol="AAPL" selected onClick={open} />
<WatchlistRow symbol="NVDA" change="+1.2%" onClick={open} />
```

Optional `change`/`changeDirection` shows a right-aligned colored figure. Stack under an uppercase "EXPLORER" section label.
