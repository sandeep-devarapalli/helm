Determinate progress / distribution bar (green by default).

```jsx
<ProgressBar current={4} total={6} showCount />
<ProgressBar current={70} total={111} tone="ink" />
```

Tones: `accent | ink | danger | warning | info`; heights `xs | sm | md`.
