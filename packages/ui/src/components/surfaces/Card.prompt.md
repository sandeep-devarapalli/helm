Surface container. `default` = quiet gray panel (stat blocks, consensus cards); `white` = white card; `accent` = green hairline for agent proposals.

```jsx
<Card tone="white" raised>…</Card>
```

`padding` = `none | sm | md | lg` (12/16/24). `interactive` adds hover feedback.
