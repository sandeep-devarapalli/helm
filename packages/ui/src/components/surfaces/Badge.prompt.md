Small semantic status badge — "Buy" consensus, run states, counts.

```jsx
<Badge tone="success">Buy</Badge>
<Badge tone="ink">Running</Badge>
```

Tones: `neutral | success | danger | warning | info | ink` (ink = filled black). `dot` adds a status dot; `mono` sets the label in mono for numeric badges. Keep labels to one or two words.
