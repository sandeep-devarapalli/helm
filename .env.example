POSTGRES_USER=helm
POSTGRES_PASSWORD=helm-local-only
POSTGRES_DB=helm
HELM_DATABASE_URL=postgresql+asyncpg://helm:helm-local-only@postgres:5432/helm
HERMES_API_KEY=helm-local-development-only
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=

# Keep provider credentials only in the ignored local .env file.
